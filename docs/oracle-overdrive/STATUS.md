# Blocky Studios Overdrive Implementation Status

Updated: 2026-07-16
Public version: `2.0.14` (frozen until the project owner confirms a successor)

| Milestone | Status | Evidence / next gate |
| --- | --- | --- |
| 0 — Baseline and architecture | **FROZEN** | Foundation and invariants documented in `MILESTONE_0.md`; do not redesign during M1–M9. |
| 1 — Shell, design system, preferences | **ACCEPTED / EXACT M3 UDT AND M9-10 INSTALLED SHELL** | Shared shell, deterministic grid, transactional Preferences, avatar/color validation, persistence/migration, focus/Escape, and responsive harness are implemented. The exact stable M3 checkout passed the requested UDT UI-integrity reload. Exact installed `2.0.14-m9-10` renders the Minecraft shell, centered Blocky Studios branding, hamburger navigation, profile menu, Replays, Curves, and Quick Apply. The later M9-9 white-panel regression was not an asset loss: the main lifecycle root was never given ownership of the canonical shell. M9-10 now attaches that shell synchronously and idempotently in main `create`/`prepare`/`show`, before asynchronous bootstrap, and retains the existing Minecraft-styled emergency surface for attachment failures. Complete dock/float/collapse/restart geometry and installed accessibility remain pending. |
| 2 — Replay data, ingestion, metadata, grid | **ACCEPTED / UDT PHYSICAL; INSTALLED MATRIX PENDING** | Protocol-v2 production bridge with v1 compatibility, deterministic v3 atomic state/recovery, persistent bounded JPEG cache, exact metadata, selectors/search, delegated input, and ID-driven virtualization are active. The stable M3 replay surface and paired Java 21 clean build passed; the fresh final mod build ran 520 tests across 76 suites with zero failures/errors/skips and produced a JAR byte-identical to the live profile copy. Exact M9-10 Replays renders its real `Waiting for an export` state and the encoded replay-search/virtual-window budgets pass. Large-grid physical frame/scroll, restart cache, metadata/thumbnail persistence, real paired-Minecraft export, and native drag remain pending. |
| 3 — Organization and file lifecycle | **ACCEPTED / EXACT-ROOT UDT VERIFIED** | Real metadata/collection actions, multi-select/manual ordering, guarded archive/delete/relink, isolated native file-operation/directory-watch services, transactional metadata replacement, rollback hardening, and lifecycle teardown are enabled. The 2026-07-16 exact-root checkpoint used cache revision `2.0.14-m9-2` and runtime-manifest SHA-256 `b3905d0e55c44438f99b23bfd6b9613a262ec15392b5c98a1f20cf25a5c2165e`. Focused M1–M3 acceptance passed 118/118; the complete JavaScript/type stack passed 420/420. UDT Debug Console was empty with `No Issues`; the final UXP-log slice had no exception, missing resource, duplicate bootstrap, or focus warning. See [M3_UI_INTEGRITY_CHECKPOINT.md](M3_UI_INTEGRITY_CHECKPOINT.md). |
| 4 — Replay viewer | **ACCEPTED / PHYSICALLY VERIFIED** | Real replay probing rejected the installed Apple ProRes 4444/AAC `.mov` in UXP video with `MEDIA_ERROR_4`, so Blocky Studios uses the verified Premiere Source Monitor path without transcoding or fake controls. Exact-path and ProjectItem ownership, bounded identity publication, play/pause, position advance, zero pause drift, distinct-media switching, safe release, and the 300 × 320 tray all passed in Premiere 26.3. Automated acceptance: 186/186 JavaScript/type checks. The exact `m4-2` UDT reload had no fresh exceptions or missing resources; unsupported volume, mute, speed, loop, seek, and frame-step controls remain honestly disabled when host/metadata capability is absent. |
| 5 — Curves | **ACCEPTED / PHYSICALLY VERIFIED** | Native Linear/Hold/Bezier and bounded numeric baked curves use real Premiere actions, exact TickTime/frame quantization, one locked transaction, verified readback, and one Undo step. The responsive graph, selectors, local editing tools, 12 immutable built-ins, versioned user presets, and honest unsupported PointF/time-basis states are enabled. Automated acceptance: 247/247 JavaScript/type checks. Exact `m5-3` UDT reload retained the complete shell with no fresh exception or missing resource; integrated Hold Apply returned verified success and a physical Premiere Undo restored endpoint interpolation from `4` to `0`. |
| 6 — Quick Apply and Recipes | **ACCEPTED / PHYSICALLY VERIFIED** | The searchable cached 537-effect index, media-aware selection, Apply Once, favorites/recents, versioned recipes, deterministic import/export, and exact post-commit component readback are active. All Premiere 26.3 `create*Action()` calls are created inside `Project.lockedAccess`; plans retain only stable intent, and multi-step recipes create every action before one locked transaction. Automated acceptance: 316/316 JavaScript/type checks. Exact `m6-6` UDT reload retained the complete shell with no fresh exception or missing resource. Live Gamma Correction apply verified 2→3 components, duplicate Apply Once was a zero-mutation no-op, one effective Premiere Undo restored 3→2, and an Extract + Color Pass recipe verified 2→4 in one transaction before one effective Undo restored 4→2. Temporary recipe/recent state was restored. |
| 7 — Multiple panels | **ACCEPTED / PHYSICALLY VERIFIED** | Four real manifest-v6 panels share one controller, store, persistence layer, Premiere gateway, bridge, native addon, and reference-counted viewer/Curves/Quick Apply coordinators. Inert `create` and active `show` hooks, root-scoped cloned DOM, duplicate-ID guards, local rollback/feedback, disconnected-root pruning, and visible emergency states are enabled. Automated acceptance: 345/345 JavaScript/type checks. Exact `m7-5` UDT reload mounted all four distinct roots in one realm with 3 dedicated mounts, no duplicate IDs, no root conflicts, and stable lease counts. Physical Quick Apply search narrowed 240→3 effects; close/reopen produced a new host window while retaining the same runtime/service identities and exactly 3 mounts. A final clean reload at 06:46 had no exception or missing resource after the expected installed-font fallback warning. |
| 8 — Command / hotkey gate | **ACCEPTED / PHYSICALLY VERIFIED** | A supported manifest-v6 command, `Open Blocky Studios Quick Apply`, invokes only `Plugin.showPanel("oracleQuickApplyPanel")` through exact plugin-ID lookup and Premiere's array-like `pluginManager.plugins` collection. The synchronous host wrapper handles every Promise rejection; no shortcut, global hook, simulated input, or fake preference exists. Exact M9-10 full JavaScript/type verification, including the M8 command suite, passed 449/449. Earlier physical UDT acceptance showed the real Premiere Window > UXP Plugins > Blocky Studios command opened the dedicated ready panel and repeated invocations retained one host window/mount. Premiere Keyboard Shortcuts returned no result for the exact command, matching Adobe's documented boundary that Premiere UXP shortcuts are unavailable. |
| 9 — Hardening, CCX, integrated acceptance | **NOT READY — EXACT M9-10 PRIVATE-LOCAL PACKAGE VERIFIED; PHYSICAL/PUBLIC GATES PENDING** | Exact `2.0.14-m9-10` source passed syntax, TypeScript, and 449/449 JavaScript tests; 6/6 Release native suites; production audit with zero vulnerabilities; and every encoded benchmark budget. Its canonical 5,334,157-byte CCX (`68082DCB170289A28453278A318CCC9577D742B75AB5E303F85DD7979F0C14A7`) verified with `PrivateLocalAcceptance=true`, `ReleaseEligible=false`; source, stage, extracted archive, and normal External install matched across 50 files with zero payload diffs. The exact install visibly rendered the shell, all main routes, a normal panel close/reopen, and all three dedicated entrypoints; Premiere then accepted a normal close and exited without force termination. This is private engineering/package acceptance, not release readiness: real-hand OLE drag/cancel/exact-position behavior, physical keyboard/focus/assistive-technology checks, UXP frame timing, ordered leak/teardown samples, a real paired-Minecraft export, full clean-installed feature/data/recovery coverage, and public asset-rights/version/signing decisions remain open. See [M9_RELEASE_EVIDENCE.md](M9_RELEASE_EVIDENCE.md). |

## Permanent release invariants

- Exact-path native OLE drag remains synchronous at threshold and independent of Premiere preparation.
- Single-click opens Source Monitor; double-click and Enter explicitly import.
- No HTML drag, playhead fallback, QE, UI automation, fake host behavior, or unsupported TrackItem label writes.
- Stable bridge identity/timestamps, strict Windows paths, deterministic v3 migration/recovery, thumbnail privacy, and linear hydration remain intact.
- The user's real Premiere project and replay files are never used for destructive validation.

## Integrated physical evidence

- All destructive operations use generated disposable media only.
- Exact private `2.0.14-m9-10` was staged, packaged by UDT 2.2.1, verified, installed through Adobe UnifiedPluginInstallerAgent at the normal External path, and matched source/stage/extraction/install across 50 files with zero payload differences. The installed shell, hamburger, profile menu, Replays, Curves, Quick Apply, normal panel close/reopen, and three dedicated entrypoints are visibly nonblank. Exact scope and residual gates are in [M9_RELEASE_EVIDENCE.md](M9_RELEASE_EVIDENCE.md).
- Plugin-data preservation, `STATE_RECOVERY_REQUIRED`, backup selection, and CCX rollback stop conditions are documented in [RECOVERY_RUNBOOK.md](RECOVERY_RUNBOOK.md).
- The current M3 exact-root UI checkpoint is recorded in [M3_UI_INTEGRITY_CHECKPOINT.md](M3_UI_INTEGRITY_CHECKPOINT.md), with screenshots and its per-file runtime hash manifest under `native/build/evidence/m3-stable-2026-07-16/`; final UXP log: `C:/Users/salim/AppData/Roaming/Adobe/Premiere Pro/Logs/UXPLogs_2026-07-16_09-15-43_884573.log`.
- M4 stable-revision evidence is retained at `native/build/m4-premiere-m4-2-shell.png`, `native/build/m4-premiere-source-monitor-viewer-open.png`, and the same UXP log markers `[M4 HARDENED VIEWER OPEN]`, `[M4 PLAY PAUSE ACCEPTANCE]`, `[M4 RAPID SWITCH CLOSE ACCEPTANCE]`, and `[M4 DISTINCT MEDIA SWITCH]`.
- M5 stable-revision evidence is retained at `native/build/m5-premiere-m5-3-desktop.png`, `native/build/m5-premiere-m5-3-curves.png`, and the same UXP log markers `[M5 M5-3 ACCEPTANCE]`, `[M5 INTEGRATED APPLY]`, and `[M5 CORRECTED UNDO READBACK]`.
- M6 stable-revision evidence is retained at `native/build/m6-m6-6-quick-apply.png`, `native/build/m6-recipe-before-effective-undo.png`, and the same UXP log markers `[M6 M6-6 READY]`, `[M6 DIRECT EFFECT APPLY M6-6]`, `[M6 APPLY ONCE NO-OP M6-6]`, `[M6 DIRECT EFFECT UNDO READBACK M6-6]`, `[M6 TWO EFFECT RECIPE M6-6]`, `[M6 RECIPE EFFECTIVE UNDO READBACK M6-6]`, and `[M6 ACCEPTANCE CLEANUP M6-6]`.
- M7 stable-revision evidence is retained at `native/build/m7-final-main.png`, `native/build/m7-final-replays.png`, `native/build/m7-final-curves.png`, `native/build/m7-final-quick-apply.png`, and `native/build/m7-quick-apply-search-crop.png`. The same UXP log records the clean 06:46 exact-revision realm `oracle-realm-mrndw70y-iee0c5pu`, four distinct panel roots, three `Blocky Studios M7 Mount Ready` events, and shared runtime/service identities.
- M8 stable-revision evidence is retained at `native/build/m8-premiere-oracle-menu.png`, `native/build/m8-command-open-quick-apply.png`, and `native/build/m8-keyboard-shortcuts-search3.png`. The same UXP log records the 06:54 manifest re-registration and the command-created `oracleQuickApplyPanel` mount in realm `oracle-realm-mrne6xsb-1gwfkc8d` without a fresh exception.
- Current M9-10 installed evidence resides under `native/build/evidence/m9-10-final-2026-07-16/`, including the shell, hamburger, profile, Replays/Curves/Quick Apply routes, normal close/reopen, and three dedicated-entrypoint screenshots. The frozen host log `UXPLogs_2026-07-16_14-14-07_521512.log` records `com.blocky.oracle.v5` added and enabled with no Blocky Studios-attributable exception or missing-resource line; unrelated Adobe/third-party records are itemized in [M9_RELEASE_EVIDENCE.md](M9_RELEASE_EVIDENCE.md).
- Historical M9-8 evidence remains under `native/build/evidence/m9-final-2026-07-16/` and is retained for the earlier bridge, command, state-preservation, and installed-panel runs. It is superseded for current artifact identity and cannot substitute for unperformed exact-M9-10 physical gates.
- Before M9-10 installation, the live External data was copied exactly (7 files, 466,766 bytes). The supported install path removed the package, the pristine six-file baseline was restored before Premiere launch with zero content differences, and no Developer-scope user state was replaced or merged.
- Public release is blocked until redistribution/commercial-use permission is
  documented for the supplied Minecraft Regular, Five, and Ten fonts plus the
  Blocky Studios logo/icon assets. Local development may use the supplied files; they
  are not yet represented as release-cleared assets.
- The project owner must confirm the successor semantic version. The retained
  private artifact remains `2.0.14`; uninstall/reinstall passed, but a true
  upgrade from `2.0.14` requires a distinct confirmed successor build.
- The Release native addon is not Authenticode-signed and this private CCX is
  not a signed public Marketplace artifact. Signing and distribution policy
  remain external release gates after asset and version policy are resolved.
